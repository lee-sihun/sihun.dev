import BlogContent from "@/components/BlogContent";

export const metadata = {
  title: "Blog",
  description: "This is the blog page containing all posts.",
};

export default function BlogPage() {
  return <BlogContent />;
}
